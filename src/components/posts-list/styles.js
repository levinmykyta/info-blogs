import { css } from 'styled-components'

export const baseStyling = () => {
  return css`
    display: flex;
    position: relative; 
    padding-bottom: 6rem;
    margin-top: 15rem;

    ol {
      display: flex;
    }
  `
}
